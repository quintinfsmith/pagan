package com.qfs.pagan

import androidx.recyclerview.widget.RecyclerView

class ColumnLabelViewHolder(itemView: ColumnLabelView) : RecyclerView.ViewHolder(itemView) {
    init {
        itemView.viewHolder = this
    }
}


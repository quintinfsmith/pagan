package com.qfs.pagan.structure.opusmanager.base.effectcontrol

enum class EffectTransition(val i: Int) {
    Instant(0),
    Linear(1),
    RInstant(2),
    RLinear(3),
    // EaseIn(4),
    // EaseOut(5)
}